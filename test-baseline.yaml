apiVersion: v1
kind: Pod
metadata:
  name: pod-baseline
  namespace: default
spec:
  runtimeClassName: kata-qemu-coco-dev
  containers:
  - args:
    - sleep
    - "360000"
    image: quay.io/bpradipt/kata-vm-image:7may
    imagePullPolicy: Always
    name: pod-baseline
